export const metadata = {
  title: "Component test · OET LMS",
  description: "Interactive preview of redesigned Radix-themed UI components used in OET LMS.",
};

export default function TestLayout({ children }: { children: React.ReactNode }) {
  return children;
}
