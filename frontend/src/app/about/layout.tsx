import { OethqFontRoot } from "@/app/website/oethq-font-root";

export default function AboutLayout({ children }: { children: React.ReactNode }) {
  return <OethqFontRoot>{children}</OethqFontRoot>;
}
